function calcular(a: number, b: number): number {
    let soma: number = a + b;
    let sub: number = a - b;
    let mul: number = a * b;
    let div: number = a / b;
    let mod: number = a % b;
    soma += 1;
    sub -= 1;
    mul *= 2;
    div /= 2;
    let eq: boolean = a == b;
    let ne: boolean = a != b;
    let lt: boolean = a < b;
    let le: boolean = a <= b;
    let gt: boolean = a > b;
    let ge: boolean = a >= b;
    let e: boolean = a && b;
    let ou: boolean = a || b;
    let nao: boolean = !a;
    a++;
    b--;
    let p: Ponto = { x: 0, y: 0 };
    p.x = soma;
    p.y = sub;
    let v: number = 4;
    v0p.x;
    v1p.y;
    v2v0 + v1;
    v3a != b++ || !v--2 < b;
    return v2;
    }
    
    